{
  "name": "@shopify-data-sync/survey-bot",
  "version": "1.0.0",
  "description": "Slack Workflow Builder からの桃アンケート回答を受け取りFirestoreに保存するWebhook",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "RUN_LOCAL_SERVER=true tsx src/index.ts",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --external:firebase-admin --external:firebase-functions --format=cjs --outfile=lib/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "tsx": "^4.21.0"
  }
}
